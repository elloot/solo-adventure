extends layout

block content
  main
    .container
      if story
        p #{story[0].body}

      if links[0]
        p Här är links
        ul
          each link in links
            if link
              li
                a(href='./' + link.target_id)= link.description
      else
        a(href='./1') Börja om